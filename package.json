{
  "name": "my-worker-project",
  "version": "1.0.0",
  "description": "Cloudflare Worker with cookie-based rate limiting, dynamic API handlers, and static asset serving",
  "type": "module",
  "scripts": {
    "generate-routes": "node generateRoutes.js",
    "build": "npm run generate-routes && wrangler build",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0",
    "@faker-js/faker": "^9.8.0",
    "axios": "^1.10.0",
    "ethers": "^6.14.4",
    "luxon": "^3.6.1",
    "tz-lookup": "^6.1.25"
  },
  "devDependencies": {
    "wrangler": "^4.20.1"
  }
}
